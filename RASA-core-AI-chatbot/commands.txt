run rasa with http request +
    rasa run --enable-api

Verification: Test the API by accessing
    curl http://localhost:5005/status
    {
  "model_file": "/path/to/model.tar.gz",
  "fingerprint": {...},
  "num_active_training_jobs": 0
}